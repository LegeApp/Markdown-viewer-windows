<UserControl x:Class="MarkdownViewer.Wpf.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="0,0,0,1" Padding="10,8" Background="LightGray">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="Open" Command="{Binding OpenFileCommand}" Margin="0,0,8,0" Padding="8,4"/>
                    <Button Content="Reload" Command="{Binding ReloadCommand}" Margin="0,0,12,0" Padding="8,4"/>
                    <TextBox Width="340" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                    <Button Content="Prev" Command="{Binding SearchPreviousCommand}" Margin="8,0,4,0" Padding="8,4"/>
                    <Button Content="Next" Command="{Binding SearchNextCommand}" Margin="4,0,0,0" Padding="8,4"/>
                </StackPanel>

                <TextBlock DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="Gray" Text="{Binding ParseStats}"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Margin="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="8">
            <FlowDocumentScrollViewer x:Name="DocumentViewer"
                                      Document="{Binding Document}"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto"
                                      IsToolBarVisible="False"
                                      Zoom="100"
                                      MinZoom="50"
                                      MaxZoom="300"/>
        </Border>

        <Border Grid.Row="2" Background="LightGray" BorderBrush="Gray" BorderThickness="0,1,0,0" Padding="12,6">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="{Binding StatusMessage}"/>
                <TextBlock DockPanel.Dock="Right" Text="{Binding CurrentFileName}" Foreground="Gray" Margin="0,0,16,0"/>
                <TextBlock DockPanel.Dock="Right" Text="Loading..." Margin="0,0,16,0"
                           Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
